import React from 'react';

const ThirdSection = () => {
    return (
        <section className="w-full h-screen flex items-center justify-center bg-white text-black">
            <h2 className="text-4xl font-bold">Next Section</h2>
        </section>
    );
};

export default ThirdSection;
